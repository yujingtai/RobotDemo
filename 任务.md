## 前置说明
> **说明：** 本任务为单一项目方向，开发周期 3 天。
> **交付物：** 代码仓库（完整 README）、运行截图/录屏、精简技术总结文档。
> **考核核心：** 工程落地能力、代码规范、异常鲁棒性、接口对接设计思路，不追求功能全覆盖，**尽力而为**。

## 项目背景
本项目为「具身智能机器人在主题邮局的应用研究」，拟建设一套基于带灵巧手人形机器人的邮政主题网点原型系统。系统以人形机器人为现场执行终端，以本地私有化边缘服务器为智能控制、推理、存储和安全枢纽，覆盖主动迎宾、业务问答、定点导览、文创商品推荐、双臂识别抓取展示、扫码选购结算、库存感知、后台监控和异常处置等环节，具备「可问、可走、可拿、可讲、可卖、可管、可审计」的闭环能力。

本任务聚焦其中**业务中台后端 + 后台管理前端 + 邮政系统对接适配层**，即「可卖、可管、可审计」三块。机器人本体的 SLAM 导航、双臂抓取、语音交互等能力在本任务中作为**外部数据源**对待——候选人仅需为其预留数据接入与状态上报接口，不需要实现机器人本体算法。系统需对接「新一代营业渠道系统平台服务子系统」提供的人形机器人寄件相关接口，但**不需要真实调用邮政生产/UAT 接口**，要求搭建完整的对接设计框架并以 Mock 方式模拟邮政侧响应。

> 参考文档（位于仓库根目录）：
> - 技术规范书：`附件1-具身智能机器人在主题邮局的应用研究的技术规范书(1).docx`
> - 邮政接口文档：`新一代营业渠道系统平台服务子系统接口文档（人形机器人寄件相关）20260611.docx`

## 任务目标
在 3 天内搭建一套可独立运行的后端业务系统及配套后台管理前端，核心达成：
1. 完成商品、订单、库存、任务、告警、用户权限、审计日志等核心数据模型设计与建表。
2. 实现商品推荐、订单创建、库存锁定/释放、支付状态同步的业务闭环。
3. 实现任务调度状态机，能编排「展示—讲解—结算—库存校验」等业务任务并处理异常分支。
4. 按邮政接口文档封装对接适配层，实现统一的会话控制（SessionHeader）与签名算法，以 Mock 模式模拟资费查询、邮件号码生成、收寄订单提交、收款二维码、支付状态查询等接口。
5. 实现后台管理前端，支持商品配置、订单查看、库存告警、任务监控、审计日志与分权登录。
6. 完成鉴权、操作留痕与断网降级的设计思路落地。

## 核心要求

#### 1. 系统架构与数据模型
- **后端技术栈**：Java 17+、Spring Boot 3.x、Spring MVC（RESTful API）、Spring Security（鉴权）、MyBatis-Plus 或 JPA（ORM，二选一）、MySQL 8.x（主数据库）、Redis（缓存与库存原子操作）、Flyway 或 Liquibase（数据库版本管理，可选）、Knife4j/SpringDoc（接口文档）。前端独立工程，与后端通过 RESTful API 交互。
- 后端采用分层结构：接口层（Controller/API）、业务服务层、对接适配层（邮政接口客户端 + Mock）、数据访问层、基础工具层（签名、鉴权、日志、异常）。
- 工程结构遵循标准 Spring Boot 分层：`controller` → `service` → `mapper`/`repository` → `entity`/`dto`/`vo`，对接适配层独立为 `adapter`/`client` 包，Mock 作为独立模块或 profile。统一异常处理（`@RestControllerAdvice`）、统一响应封装、参数校验（`@Valid`）。
- 设计并建表以下核心数据对象，字段口径统一，避免各模块不一致：
  - 商品（含标签、价格、是否支持机器人抓取展示、陈列点位）
  - 库存（实时库存、锁定库存、低库存阈值、样品缺失/错位标记）
  - 订单（订单号、商品明细、金额、状态、关联邮件号码、支付流水号）
  - 支付（支付流水号、二维码链接、支付状态、查询流水号）
  - 任务（任务编号、类型、状态、优先级、依赖、超时阈值、失败原因、关联日志）
  - 告警（告警类型、级别、来源、状态、处理记录）
  - 用户与权限（角色：运营/维护/管理员，权限项）
  - 审计日志（操作人、时间、操作类型、对象、结果、关联流水号）
  - 知识库（可选，问答条目与版本号）
- 数据库设计要求：每张表含主键、创建时间、更新时间、逻辑删除标志；金额字段用 `DECIMAL`，状态字段用枚举字典并注释；库存锁定/释放使用 Redis 分布式锁或数据库乐观锁保证并发安全。
- 全部可配置项（数据库连接、邮政接口地址、签名密钥、机构编码、平台编码、超时阈值、库存阈值、推荐规则）统一写入配置文件，禁止硬编码。
- 配置项统一写入 `application.yml` 并支持环境变量覆盖，不同环境（dev/test/prod）通过 Spring profile 切换；邮政接口 Mock 与真实地址通过配置开关切换，业务代码不感知差异。

#### 2. 邮政系统对接适配层（重点）
- 按接口文档实现统一的请求封装与响应解析，所有邮政接口共用同一套会话控制与签名机制：
  - 请求结构为 `YYRoot`，包含 `SessionHeader` 与 `SessionBody`。
  - `SessionHeader` 至少包含：`ServiceCode`（F8）、`Version`（如 `YY-1.0`）、`ActionCode`（请求填 `0`）、`TransactionID`、`SrcSysID`（发起方编码）、`DstSysID`（固定值 `XYDYYQDXT`）、`DigitalSign`、`ReqTime`（`yyyyMMddHHmmss`）。
  - `TransactionID` 格式：`【5位平台编码】+【17位日期 yyyyMMddHHmmssfff】+【10位流水号】`，需全局唯一。
  - 签名算法：将 `ServiceCode+Version+ActionCode+TransactionID+SrcSysID+DstSysID+ReqTime+（SessionBody 内容）+秘钥` 拼接后做 MD5，再做 BASE64 编码，作为 `DigitalSign`。
- 封装以下接口的请求/应答结构（以 Mock 模式模拟邮政侧返回，不真实调用）：
  1. **邮件资费查询接口**：入参含产品代码、收寄省/市/区县、寄达省/市/区县、是否保价、保价金额、长宽高、重量（克）等；应答返回邮资费用。
  2. **邮件号码生成服务**：入参含省份代码、机构编号、业务产品代码/名称、数据来源代码等；应答返回生成的邮件号码。
  3. **业务办理-收寄订单提交服务**：入参含机构/员工/寄件人/收件人信息、业务产品、邮件条码、重量、件数、资费、包装物列表、内件信息列表等；应答返回交易流水号、总资费、应收总资费、资费明细。
  4. **生成订单收款二维码接口**：入参含机构编号、台席代码、员工工号、查询流水号；应答返回平台流水号、支付流水号、二维码链接。
  5. **支付状态查询服务**：入参含查询流水号、机构编号、支付流水号；应答返回支付状态（`01` 成功 / `02` 失败 / `03` 已退款 / `05` 部分退款 / `00` 支付中）。
- Mock 实现要求：
  - 提供独立的 Mock 服务或本地桩，能够按接口文档返回结构正确的报文，并支持模拟成功、失败、超时、签名错误等场景，便于联调与异常分支验证。
  - Mock 需模拟支付状态的流转（如支付中 → 支付成功），支撑订单状态机联调。
  - 适配层与 Mock 之间通过配置开关切换，真实接口地址与 Mock 地址均由配置驱动，业务代码不感知差异。
- 健壮性要求：
  - 统一处理邮政侧错误码（参见接口文档返回编码表，如 `9009` 报文解密错误、`1002` 签名错误、`1006` 访问量超限等），映射为本地错误码并记录日志。
  - 接口调用需支持超时、重试（可配置次数）、幂等（以 `TransactionID` 去重）、签名校验失败告警。
  - 断网或邮政侧不可达时，本地维持业务可用（如订单可创建、二维码可展示、支付状态可本地暂存），链路恢复后补偿同步，并保留处理记录。

#### 3. 商品与库存模块
- 商品管理：新增、编辑、上下架、标签维护、陈列点位配置、是否支持机器人抓取展示标记。
- 商品推荐：根据客户需求标签、商品标签、库存状态、活动规则生成推荐结果，结果需包含可解释的推荐理由，并标识是否支持机器人抓取展示。
- 库存管理：实时库存维护；订单创建时锁定库存，支付成功后扣减，支付取消/失败/超时后释放；低库存阈值告警；支持视觉库存校验结果回写（样品缺失、陈列错位、账实不一致）并与推荐结果联动。

#### 4. 订单与支付模块
- 订单状态机至少覆盖：待支付、支付中、支付成功、支付失败、已取消、已超时、需人工处理，避免重复确认与库存异常。
- 订单创建后调用邮政收款二维码接口展示二维码；通过支付状态查询接口轮询或回调更新订单状态，支付结果明确后扣减或释放库存。
- 订单与邮件号码、资费、支付流水号关联，全链路可追溯。
- 高敏支付信息本地不存储（如不落盘完整支付凭证），仅保留必要的状态与流水号。

#### 5. 任务调度与状态机
- 实现统一任务状态机，状态至少包括：`CREATED`、`QUEUED`、`RUNNING`、`PAUSED`、`SUCCEEDED`、`FAILED`、`CANCELLED`、`MANUAL_REQUIRED`。
- 支持任务优先级（安全类最高、客户交互次之、后台巡检最低）、任务依赖（如抓取展示依赖导航到位与商品识别）、超时阈值与重试次数。
- 连续失败时停止自动重试并转 `MANUAL_REQUIRED`，避免不可控循环。
- 每个任务记录唯一编号、输入输出、耗时、状态变化、失败原因与关联日志，支撑审计与运维分析。
- 机器人本体能力（导航到位、抓取结果、语音意图）通过预留的状态上报接口接入，候选人可用 Mock 数据驱动任务流转，无需实现机器人端。

#### 6. 后台管理前端（辅）
- 提供后台管理界面，至少包含：登录与分权（运营/维护/管理员）、商品配置、订单列表与详情、库存与告警、任务监控（任务列表、状态、失败原因）、审计日志查询、用户与权限管理。
- 界面需为工具型后台风格：信息密度合理、便于扫描与重复操作，避免营销式大图与装饰性卡片堆叠。
- 推荐使用主流前端框架（Vue 3 或 React 任选）独立工程，与 Spring Boot 后端通过 RESTful API 交互；可使用 Element Plus / Ant Design 组件库。
- 任务监控与告警需支持实时或近实时刷新（轮询或 WebSocket 均可）。

#### 7. 鉴权与审计
- 基于 Spring Security + JWT 实现登录鉴权与基于角色的权限控制（RBAC），区分运营、维护、管理员三类角色，接口级鉴权（`@PreAuthorize` 或自定义过滤器）。
- 关键操作留痕：用户登录、权限变更、地图/知识库发布、订单状态变更、支付回调、邮政接口调用、异常告警均需写入审计日志，记录操作人、时间、对象、结果与关联流水号。
- 审计日志支持按时间、操作类型、操作人检索与导出。

#### 8. 工程规范
- 提供一键启动方式：`docker-compose up` 拉起 MySQL、Redis、Mock 服务；Spring Boot 后端通过 `mvn spring-boot:run` 或打包 `java -jar` 启动；前端 `npm run dev`。建议在 README 给出完整启动顺序。
- 配置项全部外置（配置文件或环境变量），禁止硬编码接口地址、密钥、机构编码、阈值。
- 代码分层清晰：接口层、业务服务层、对接适配层、数据访问层、工具层；注释规范，无冗余打印。
- 日志分级输出，异常场景打印清晰故障信息，系统异常不崩溃、可接收新请求。
- 基于 Spring Boot Test（`@SpringBootTest`）+ JUnit 5 提供基础单元/集成测试，至少覆盖签名算法、订单状态机、库存锁定/释放、邮政接口 Mock 联调。

## 预期效果
后端业务系统可独立运行，后台管理前端可登录并完成商品配置、下单、支付状态流转、库存联动、任务监控与审计查询；邮政对接适配层在 Mock 模式下完整走通资费查询、邮件号码生成、收寄订单提交、收款二维码、支付状态查询五个接口，签名与会话控制符合接口文档规范；订单与任务状态机在各类异常分支（超时、失败、签名错误、断网）下能正确流转并留痕，无进程卡死。技术文档清晰说明系统架构、数据模型、对接设计、Mock 策略与断网降级思路。

## 通用交付要求
| 交付项 | 说明 |
|---|---|
| 代码仓库 | README 写明环境依赖、一键启动命令、模块说明、邮政对接适配层与 Mock 切换方式 |
| 运行截图/录屏 | 后台管理界面操作、订单支付状态流转、任务监控、邮政接口 Mock 联调、异常分支演示 |
| 技术总结文档 | 系统架构图、数据模型设计、对接设计思路（会话控制/签名/幂等/降级）、开发踩坑与未完成项如实说明 |

> 补充考核提示：如实记录未完成模块、受限原因，完整真实的工程记录优于虚假全功能实现。3 天内优先保证「订单支付闭环 + 邮政对接适配层 + 后台管理」主干跑通，知识库、数据分析报表等可作为加分项。
