spring:
  datasource:
    url: jdbc:h2:mem:robotdemo;MODE=MySQL;DB_CLOSE_DELAY=-1
    username: sa
    password:
    driver-class-name: org.h2.Driver
  flyway:
    enabled: false

postal:
  mock-enabled: true
